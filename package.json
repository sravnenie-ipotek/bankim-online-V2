{
  "name": "bankimonline-turbo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=api --filter=web-next",
    "dev:legacy": "turbo dev --filter=api --filter=web",
    "build": "turbo build --filter=api --filter=web-next",
    "build:legacy": "turbo build --filter=api --filter=web",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "engines": {
    "node": ">=20.x"
  },
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "react-player": "^3.4.0"
  }
}

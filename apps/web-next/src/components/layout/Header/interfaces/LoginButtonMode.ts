/**
 * Visual mode for the header login button.
 * - normal: accent background, primary text (default).
 * - black: black background, white text (e.g. vacancies page).
 */
export type LoginButtonMode = 'normal' | 'black';

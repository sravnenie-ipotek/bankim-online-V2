/**
 * Props for the vacancy category badge (job card category pill with yellow bullet).
 */
export interface VacancyCategoryBadgeProps {
  /** Translated category name to display (e.g. from getContent('vacancies_category_development')). */
  categoryLabel: string;
}

import type { VideoControlBarProps } from './VideoControlBarProps';

/**
 * Props for VideoControlBar: a single config object containing all control bar parameters.
 */
export interface VideoControlBarComponentProps {
  config: VideoControlBarProps;
}

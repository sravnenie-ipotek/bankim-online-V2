export interface PercentIconProps {
  /** Optional class for size/alignment (default: w-[clamp(40px,4.722vw,68px)] h-[clamp(40px,4.722vw,68px)] self-start) */
  className?: string;
  /** Optional image source for one shared icon file. */
  iconSrc?: string;
}

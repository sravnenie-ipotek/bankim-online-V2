'use client';

import React from 'react';

/**
 * Content shown under the General questions tab (contact details).
 * Add copy, links, or other UI here.
 */
const GeneralQuestionsContent: React.FC = () => (
  <div className="flex flex-col gap-2 w-full pt-2 border-t border-base-stroke mt-2">
    <p className="text-sm text-textTheme-secondary">
      {/* Placeholder: replace with getContent or static text */}
    </p>
  </div>
);

export default GeneralQuestionsContent;
